<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.ziwei</groupId>
    <artifactId>ziwei-doushu</artifactId>
    <version>1.0.0</version>
    <name>ziwei-doushu</name>
    <description>紫微斗数排盘引擎 — Java / Spring Boot 移植版（算法与参数严格对齐原 TypeScript 项目）</description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!--
          农历/公历转换：原项目用 lunar-javascript / lunar-lite。
          Java 端建议使用同一作者 6tail 的孪生库 cn.6tail:lunar-java，以保证日历算法一致。
          排盘引擎（ZiweiEngine，对应 iztro 的 lib/star/location.js）尚在移植中，
          待引擎接入后再启用此依赖（请按 Maven 中央仓库的实际可用版本号填写）：
        <dependency>
            <groupId>cn.6tail</groupId>
            <artifactId>lunar-java</artifactId>
            <version>1.7.4</version>
        </dependency>
        -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
